import SwiftUI

struct HorizontalAlignmentLeading: View {
    var body: some View {
        Text("Hello, SwiftUI!") // Example of a SwiftUI view content
            .font(.largeTitle)
            .foregroundColor(.blue)
    }
}
